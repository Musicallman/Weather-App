{
  "name": "spa-app",
  "version": "0.0.1",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "node server.js",
    "start": "npm run client && npm run server"
  },
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.16.2"
  },
  "devDependencies": {
    "concurrently": "^3.5.0",
    "node-fetch": "^2.1.2"
  }
}
